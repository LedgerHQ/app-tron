[pytest]
# Anchor the rootdir here so ragger writes snapshots under tests/ (not the repo root),
# and expose this directory on sys.path so `application_client` / `utils` import cleanly.
pythonpath = .
